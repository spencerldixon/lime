# Copy to ~/.config/lime/config.yaml (or $XDG_CONFIG_HOME/lime/config.yaml).
# Both settings are optional; these are the defaults.

width: 88             # Content width in columns; the document is centred in the window
vertical_padding: 3   # Blank rows above and below the document
